{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('assets/css/base.css') }}" type="text/css">
{% endblock %}
{% block body %}
<style>
 
</style>

<h1 class="text-center m-3 titre">PARIS: <i>LA VILLE LUMIÈRE</i></h1>
<div class="example-wrapper d-flex justify-content-around">
    
    {% for product in Paris %}
    <div class="card w-25 m-4">
       <img src=" img/{{ product.image}}" class="card-img-top h-50" alt="{{ product.image}}">
    <div class="card-body">
       <h5 class="card-title">{{product.title}}</h5>
       <p class="card-text"> {{product.description}}</p>
       <p class="card-text"><small class="text-muted">{{ product.date_post }}</small></p>
  </div>
  </div>
  {% endfor %}
 </div> 
{% endblock %}
